{
  "name": "abastece",
  "version": "1.0.0",
  "private": true,
  "description": "Abastece — mapeamento colaborativo de preços de combustíveis no Brasil.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "web": "npm run dev --workspace @abastece/web",
    "mobile": "npm run start --workspace @abastece/mobile",
    "pull": "git pull --rebase origin main",
    "sync": "powershell -ExecutionPolicy Bypass -File scripts/sync.ps1"
  }
}
